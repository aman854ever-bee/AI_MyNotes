# shared-types

Placeholder. `apps/web/src/types.ts` holds the types for now; once
`services/api` needs the same shapes (Phase 4+), move them here and wire
both sides to this package instead of duplicating.
